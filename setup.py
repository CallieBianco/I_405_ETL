from distutils.core import setup
from Cython.Build import cythonize

#setup(ext_modules = cythonize('ETL_SIM_c.pyx'))
#setup(ext_modules = cythonize('bus_c.pyx'))
#setup(ext_modules = cythonize('car_c.pyx'))
#setup(ext_modules = cythonize('file_saver_c.pyx'))
#setup(ext_modules = cythonize('highway_c.pyx'))
#setup(ext_modules = cythonize('exit_c.pyx'))
#setup(ext_modules = cythonize('enter_c.pyx'))
#setup(ext_modules = cythonize('car_2_c.pyx'))
#setup(ext_modules = cythonize('car_move_test_c.pyx'))
#setup(ext_modules = cythonize('car_tests_c.pyx'))
#setup(ext_modules = cythonize('Price_Elasticity_Model.pyx'))